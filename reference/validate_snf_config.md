# Validator for snf_config class object

Validator for snf_config class object

## Usage

``` r
validate_snf_config(scl)
```

## Arguments

- scl:

  An SNF config-like `list` class object.

## Value

If dll has a valid structure for a `data_list` class object, returns
input unchanged. Otherwise, raises an error.
