# Constructor for `ext_solutions_df` class object

Constructor for `ext_solutions_df` class object

## Usage

``` r
new_ext_solutions_df(ext_sol_dfl)
```

## Arguments

- ext_sol_dfl:

  An extended solutions data frame-like object.

## Value

An `ext_solutions_df` object, which is a data frame with class
`ext_solutions_df`.
