# Convert an object to an ARI matrix

This function coerces non-`ari_matrix` class objects into `ari_matrix`
class objects.

## Usage

``` r
as_ari_matrix(x)
```

## Arguments

- x:

  The object to convert into a weights matrix.

## Value

An `ari_matrix` class object.
