# Add "uid\_" prefix to all UID values in uid column

Add "uid\_" prefix to all UID values in uid column

## Usage

``` r
prefix_dll_uid(dll)
```

## Arguments

- dll:

  A data list-like `list` class object.

## Value

A data list with UIDs prefixed with the string "uid\_".
