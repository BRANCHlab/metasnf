# Helper function to pluralize a string

Helper function to pluralize a string

## Usage

``` r
pl(x)
```

## Arguments

- x:

  A vector of length 1 or greater.

## Value

A string "s" if the length of x is greater than 1, otherwise an empty
string.
