# Convert an object to a data list

This function coerces non-`data_list` class objects into `data_list`
class objects.

## Usage

``` r
as_data_list(x)
```

## Arguments

- x:

  The object to convert into a data list.

## Value

A `data_list` class object.
