# Bar plot separating a feature by cluster

Bar plot separating a feature by cluster

## Usage

``` r
bar_plot(df, feature)
```

## Arguments

- df:

  A data.frame containing cluster column and the feature to plot.

- feature:

  The feature to plot.

## Value

A bar plot (class "gg", "ggplot") showing the distribution of a feature
across clusters.
