# Convert a similarity matrix to a dissimilarity matrix

Convert a similarity matrix to a dissimilarity matrix

## Usage

``` r
sim_to_dist(sim_mat)
```

## Arguments

- sim_mat:

  A similarity matrix

## Value

A dissimilarity matrix
