# Check if clustering functions list-like object has named algorithms

Check if clustering functions list-like object has named algorithms

## Usage

``` r
check_cfll_named(cfll)
```

## Arguments

- cfll:

  A clust_fns_list-like `list` class object.

## Value

Doesn't return any value. Raises error if there are unnamed clustering
functions in cfll.
