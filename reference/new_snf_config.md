# Constructor for `snf_config` class object

Constructor for `snf_config` class object

## Usage

``` r
new_snf_config(scl)
```

## Arguments

- scl:

  An SNF config-like `list` class object.

## Value

An `snf_config` object.
