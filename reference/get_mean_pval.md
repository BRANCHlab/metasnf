# Get mean p-value

Given an solutions data frame row containing evaluated p-values, returns
mean.

## Usage

``` r
get_mean_pval(sol_df_row)
```

## Arguments

- sol_df_row:

  row of sol_df object

## Value

mean_pval mean p-value
