# Convert a data list into a data frame

**\[deprecated\]** Defunct function for converting a data list into a
data frame. Please use
[`as.data.frame()`](https://rdrr.io/r/base/as.data.frame.html) instead.

## Usage

``` r
collapse_dl(data_list)
```

## Arguments

- data_list:

  A nested list of input data from `generate_data_list()`.

## Value

A "data.frame"-formatted version of the provided data list.
