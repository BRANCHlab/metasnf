# Convert an object to a settings data frame

This function coerces non-`settings_df` class objects into `settings_df`
class objects.

## Usage

``` r
as_settings_df(x)
```

## Arguments

- x:

  The object to convert into a data list.

## Value

A `settings_df` class object.
