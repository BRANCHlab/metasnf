# Check if functions in a distance metrics list-like have valid arguments

Check if functions in a distance metrics list-like have valid arguments

## Usage

``` r
check_cfll_fn_args(cfll)
```

## Value

Doesn't return any value. Raises error if the functions in dfll don't
have valid arguments.
