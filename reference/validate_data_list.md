# Validator for data_list class object

Validator for data_list class object

## Usage

``` r
validate_data_list(dll)
```

## Arguments

- dll:

  A data list-like `list` class object.

## Value

If dll has a valid structure for a `data_list` class object, returns the
input unchanged. Otherwise, raises an error.
