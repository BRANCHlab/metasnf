# Convert an object to a snf config

This function coerces non-`snf_config` class objects into `snf_config`
class objects.

## Usage

``` r
as_snf_config(x)
```

## Arguments

- x:

  The object to convert into a snf config.

## Value

A `snf_config` class object.
